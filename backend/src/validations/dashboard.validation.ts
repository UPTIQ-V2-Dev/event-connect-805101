const getStats = {
    // No query parameters or body needed for dashboard stats endpoint
    // Only requires authentication via headers which is handled by auth middleware
};

export default {
    getStats
};
